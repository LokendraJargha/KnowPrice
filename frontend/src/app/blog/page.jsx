import WidthWrapper from "@/components/common/WidthWrapper";
import BlogsList from "@/components/section/BlogsList";

import React from "react";

const page = async () => {

  return (
    <WidthWrapper>
      <BlogsList />
    </WidthWrapper>
  );
};

export default page;
